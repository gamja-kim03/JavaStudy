1. 캡슐화는 알멩이와 껍질로 나뉘는데 알멩이는 데이터(속성)을 말하며 껍질은 메소드를 말함
2. 설계적으로 봤을 때 캡슐화는 접근지정자인 private안에 int wheel(멤버변수)이 있으면 값을 수정하는 것도 안되고(input), 값을 불러오는 것도 안됨(output)
3. 만약 보호되어있는 int wheel을 쓰고싶으면 return을 써서 불러와야함
4. this
5. default는 자바에서만 있음
6. setter 