- 메소드 정의
public static int func
                (int i, string j)
{ return 10 ;}


- 함수는 정의된 후 클래스 밖에서 호출되어야 실행됨 / 정의되야 호출할 수 있음음

- 자바는 메인 함수가 호출 되여야한다고 약속함 즉 시작점


- return은 한 개만 할 수있으며 반환형도 한 개만 가능하며 리턴형에 객체는 안되지만 그 타입인 클래스는 가능하며 리턴은 객체의 주소가 리턴됨
